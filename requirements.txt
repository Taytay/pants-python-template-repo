# (The original requirements.txt file came from the Pants project template)

# After you add a new requirements, don't forget to re-run `scripts/pants-refresh-after-python-update.sh`

ansicolors==1.1.8
setuptools>=56.2.0,<57
types-setuptools>=56.2.0,<58
pytest==7.1.3
click==8.1.7
numpy==1.26.4

